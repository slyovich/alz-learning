/**
 * Angular environment flag.
 *
 * All Entra ID / API configuration has been externalised to
 * /config.json (loaded at runtime). See src/app/config/app-config.ts.
 */
export const environment = {
  production: false,
};
